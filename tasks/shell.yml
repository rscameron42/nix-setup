- name: set zsh as default shell for target user (Linux)
  user:
    name: "{{ target_user }}"
    shell: /usr/bin/zsh
  become: true
  when: ansible_facts['os_family'] != "Darwin"

# macOS: zsh has been the default shell since Catalina (10.15) — no action needed
